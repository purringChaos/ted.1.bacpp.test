package tv.blackarrow.cpp.model.scte224;

//Only These are the 3 statuses we will be getting from runtime. This is same as added in Hosted Layer
public enum SCTE224EventStatus {
	CONFIRMED, PENDING, ERROR, STOP_NOW;
}
