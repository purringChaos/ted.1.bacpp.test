package test.tv.blackarrow.cpp.purger;

import org.junit.Test;

import tv.blackarrow.cpp.purger.Purger;

public class PurgerTest {

    @Test
    public void testPurgeUnneededData() {
        // FIXME add real testing for the file cleanup process
        // for now just making sure it doesn't blow up when run
        Purger.purgeUnneededData();
    }

}
